# github-test-repo

Simple Burla demo repository used to verify GitHub push flow.

## What it does

- Defines a picklable function `cube_number`
- Validates locally
- Runs remote fan-out with `remote_parallel_map(..., grow=True)`

## Run

```bash
python burla_basic_demo.py
```

